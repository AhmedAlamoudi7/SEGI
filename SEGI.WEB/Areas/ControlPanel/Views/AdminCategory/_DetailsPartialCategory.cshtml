<div class="card shadow-sm border-0 rounded-lg">
    <div class="row g-0">
        <!-- Left: Image section -->
 
        <!-- Right: Details section -->
        <div class="col-md-12">
            <div class="card-body">
                <h2 class="card-title text-dark">@Model.Name</h2>
@*                  <p class="text-muted">Created on: @Model.CreatedAt.ToString("MMMM dd, yyyy")</p>
 *@            </div>
        </div>
    </div>
</div>
